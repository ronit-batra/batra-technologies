[build]
  command = "npm run build"
  publish = "out"

# Proxy all /api/* requests to your ngrok tunnel (permanent URL)
[[redirects]]
  from = "/api/*"
  to = "https://mandarin-ramp-freely.ngrok-free.dev/api/:splat"
  status = 200
  force = true
